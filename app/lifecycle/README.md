# lifecycle

Phase 7 — MLOps & Production. Vector CRUD/delete/sync · dedup · incremental · embedding migration.

| File (sẽ build) | Kỹ thuật |
|---|---|
| `reembed.py` | re-embed toàn kho khi đổi embedding model (migration, không mất dữ liệu) |

Drift detection (PSI/KL divergence giữa phân bố embedding tuần này vs tuần trước) — code tay
trước khi dùng tool, xem roadmap Phase 7.
